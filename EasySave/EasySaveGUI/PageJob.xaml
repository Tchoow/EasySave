<Page x:Class="EasySaveGUI.PageJob"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EasySaveGUI"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Page1">

    <Grid Background="White">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="250" />
                <RowDefinition Height="250" />
            </Grid.RowDefinitions>
            <DataGrid x:Name="myDataGrid" AutoGenerateColumns="False" Width="600" Height="250">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="*" />
                    <DataGridTextColumn Header="Names" Binding="{Binding Names}" Width="*" />
                    <DataGridTextColumn Header="SourcePath" Binding="{Binding SourcePath}" Width="*" />
                    <DataGridTextColumn Header="DesPath" Binding="{Binding DesPath}" Width="*" />
                    <DataGridTextColumn Header="State" Binding="{Binding State}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
            <DataGrid VerticalAlignment="Bottom" Height="500" Background="Red" Margin="0,200,0,50" Grid.RowSpan="2">
                <Label Content="TEST" />
            </DataGrid>
        </Grid>
    </Grid>
</Page>
