using Microsoft.VisualStudio.TestTools.UnitTesting;
using EasySave;
namespace TestUnit
{
    [TestClass]
    public class JobTest
    {
        [TestMethod]
        public void JobString()
        {
            /*
            string expected = "good";
            string res = "";

            Job test = new Job("a", "a", "a", 1);
            res = test.method();


           Assert.AreEqual(expected, res);
           */
        }
    }
}
